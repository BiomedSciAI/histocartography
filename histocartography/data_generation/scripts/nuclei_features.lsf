#!/bin/bash

#BSUB -q prod.med
#BSUB -n 1
#BSUB -R "rusage [ngpus_excl_p=1]"

module load Miniconda3
conda activate histog

#BSUB -J "log_benign"
#BSUB -o "log_benign"
#BSUB -e "log_benign.stderr"

python3 -u main.py --mode=features_cnn --data_param=dataT --is_train=False --is_test=True --is_analysis=False --encoder=resnet50 --is_mask=False --tumor_type=benign
